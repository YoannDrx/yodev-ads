# Lot 17 — qualification du cockpit et statut public

7 septembre 2026. Contrat : [Cockpit et statut public](../../COCKPIT_AND_PUBLIC_STATUS.md). Changements locaux sur `codex/prod-ready`, aucune migration supplémentaire, aucun appel fournisseur, envoi externe ni déploiement.

- `check.log` : contrôle complet réussi, **1 226 tests / 166 fichiers**, cinq tests de release, lint, types, frontières de données, sérialisation, build et audit runtime à zéro vulnérabilité. Couverture : 92,40 % instructions, 86,83 % branches, 93,17 % fonctions, 95,12 % lignes.
- `final-ui-check.log` : lint/build final après remplacement de l’arc décoratif par la représentation du score réel et la jauge neutre sans score.
- `database.log` : suite PostgreSQL complète avec les 53 migrations existantes. Le compteur du cockpit retrouve 521 alertes sélectionnées malgré 301 plus récentes sur un autre client ; les états acquitté/reporté/résolu sont exclus et la frontière tenant est exercée. La fixture publique parcourt 502 incidents et 701 messages à la microseconde, sans perte/doublon, avec insertion concurrente, portée des curseurs, filtres, masquage des incidents privés et incident actif âgé de 400 jours.
- `browser.log` : recette générale **40 réussites, un échec, aucun skip** sur 41 scénarios. Les nouveaux parcours du cockpit et du registre réussissent en FR/EN, à 390/1440 px. L’échec isolé était le sélecteur partiel `getByText('État du service')` devenu ambigu avec les nouvelles mentions d’état non vérifié. Le diagnostic original est conservé dans `initial-status-selector-failure.md`.
- `browser-status-final.log` : après sélection exacte et assertion explicite de non-opérationnalité, les **trois scénarios concernés réussissent** en 4,9 secondes. Les 40 autres scénarios n’ont pas été rejoués après ce changement limité au test. Tous les scénarios de la matrice disposent ainsi d’une réussite sur cette version du code applicatif, sans prétendre que le premier passage général était vert.
- Deux tests de rendu serveur supplémentaires dans `public-status-page.test.tsx` provoquent une erreur de stockage FR/EN et vérifient message d’indisponibilité, absence de statut opérationnel et absence de couleur verte. Ils passent après les contrôles finaux de lint/types. Ils s’ajoutent aux 1 226 du passage complet ; celui-ci n’a pas été rejoué pour ces deux assertions de test uniquement.
- Captures : statut inconnu, historique public, cockpit sans collecte et score indéterminé, initialisation de la page de connexion. Les vues FR mobile et EN desktop représentatives ont été inspectées ; aucun débordement horizontal dans les assertions navigateur.

Les fixtures et serveurs navigateur sont nettoyés. La base locale native reste celle utilisée pour les recettes du projet. Le statut `unknown` est volontaire : aucune sonde fournisseur réellement reçue n’a été produite dans ce lot. La recette déployée, l’observabilité externe et la bêta restent à réaliser.
