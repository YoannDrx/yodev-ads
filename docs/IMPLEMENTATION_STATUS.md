# Ads by Yodev — implementation status

Last reviewed: 2026-08-18. This is the launch-readiness source of truth. A UI is not sufficient: “implemented” means the server contract, isolation and automated verification exist. External validation and rollout evidence are tracked separately.

Status meanings:

- **Implemented**: implemented in the repository and covered by automated checks.
- **Integration pending**: code is implemented; a real staging provider, migration, legal review, load test or operational drill is still mandatory.
- **Partial**: a useful slice exists, but the roadmap contract is incomplete.
- **Planned**: not implemented.

## Commercial Safe Launch (phases 0–7)

| Area | Repository status | Mandatory gate before commercial production |
| --- | --- | --- |
| Feature flags and kill switches | Implemented | Keep production flags off until each integration is validated. Global, per-workspace and independent status/budget/keyword/ad/batch mutation switches fail closed. |
| Dependencies, headers, CSP, audits | Implemented | Exercise CSP on every Better Auth/public route. CI now scans the full Git history with Gitleaks and publishes separate 30-day CycloneDX runtime SBOM artifacts for the web app and Python CLI. |
| Transactional Postgres and tenant repository | Implemented on staging | Interactive transactions and tenant/system roles exist. Runtime pools are cached per credential URL, bounded by `DATABASE_POOL_MAX`, and preserve typed Drizzle inference across Neon and the PostgreSQL integration adapter. A CI verifier forbids concurrent `Promise.all` work inside one transaction callback, preventing unsupported multi-query use of a single `pg` client. All App Router source files use repository/service methods and have no direct Drizzle/schema/transaction imports. Tenant lifecycle and sensitive mutations remain transactional and audited. Migrations through `0041` are applied to the isolated EU staging database. The pre-migration restore branch `backup-pre-0041-20260817` is retained; all 35 prior migrations and table counts were verified before the controlled application of `0035`–`0041`. No production migration has been run by Codex. |
| RLS, DB roles, composite tenant constraints and immutable audit | Implemented | On 2026-08-17 a fresh PostgreSQL 17 with migrations through `0041` passed 46-table RLS/FORCE RLS checks, four no-`BYPASSRLS` roles including the isolated `yodev_auth` role, actor-isolated OAuth sessions, immutable-audit privileges, 33 composite constraints and 15 tenant/auth invariants. The EU staging branch is now also on `0041` after a backed-up controlled migration. Better Auth obtains only a membership priority and member quota through two public-schema functions revoked from `PUBLIC`; both verify user membership, and the auth role still cannot query tenant workspace state directly. The restricted-role proof returns internal priority/limit for the member and neutral values for an outsider. Global subprocessor-change notices are denied to the application and purge roles and granted only to the system role. CI repeats the database protocols. Production rollback remains a cutover gate. |
| Better Auth identity and permission matrix | Integration pending | Clerk runtime code and dependencies are removed. Email/password with mandatory verification, magic link, Google sign-in, passkeys, password reset, sessions, organizations, five app roles (`owner`, `admin`, `strategist`, `analyst`, `client`), invitations, ownership transfer and session revocation are implemented. Les anciens rôles `operator`/`viewer` sont migrés vers `strategist`/`client` et restent lus de façon rétrocompatible pendant la bascule. Invitation delivery is a durable, idempotent retrying job. The guarded one-time import was executed for both existing workspaces. On 2026-08-17, five isolated authenticated sessions passed the real staging page-authorization matrix 5/5 and all temporary identities, sessions and credentials were deleted. The direct Server Action/API matrix, full FR/EN lifecycle and Better Auth Google callback drill remain before cutover. |
| Lifecycle, one-time trial and entitlements | Implemented | Page navigation, direct Server Actions, API keys and public reports fail closed by lifecycle state. Grace exposes stored views plus billing without Google calls; suspended/deletion states expose billing, export and deletion only. The one-time trial identity is migrated from the legacy Clerk identifier to the Better Auth user without making repeated trials possible. Test concurrent workspace creation and every state transition on authenticated staging. |
| Stripe lifecycle and tax fail-closed | Integration pending | Le Price Stripe récurrent EUR/mois unique est l’unique autorité du forfait ; métadonnées incohérentes, Price inconnu, lignes multiples, devise/intervalle/quantité invalides déclenchent une réconciliation et bloquent le nouveau Checkout. Un upgrade reste demandé sans nouveaux droits jusqu’à `invoice.paid`; un downgrade programmé conserve les droits jusqu’à l’échéance puis applique forfait, quotas, audit et sync atomiquement sans supprimer de données. Les événements checkout/subscription/pending-update/schedule/invoice/refund sont réclamés avant traitement, rejouables après échec et relisent l’état Stripe autoritatif. Le catalogue sandbox actif est un produit/3 Prices EUR 29/89/189 et le changement de plan est géré dans l'application, le portail Stripe restant limité à la carte, aux factures et à l'annulation. Un drill sandbox réel a validé Solo → Studio payé, Studio → Solo programmé puis annulé. Le compte disponible est toutefois test/US et désactivé ; Test Clock complet, compte live français/KYC, achat/refund réel et validation fiscale restent obligatoires. |
| FR/EN and consent | Implemented slice | Public/legal content, landing, onboarding, Better Auth flows, metadata and the authenticated customer product are localized, including cockpit, analysis, generated findings, insights, history, approvals, alerts, tasks, reports, billing and settings. The locale chosen before signup is inherited atomically by the new workspace, can be changed by an administrator, and drives report OTP/lifecycle/task/support emails, alert emails, connector payloads and weekly digests. Legacy Server Action flash/error literals are translated centrally at render time; unknown provider diagnostics are preserved verbatim. The internal-only Yodev operations console remains intentionally French. Validate the complete English journey in authenticated staging E2E. |
| Commercial documents | Integration pending | Le parcours achetable et les CGV sont désormais exclusivement B2B ; raison sociale, pays, preuve d’acceptation CGV/confidentialité/DPA et versions exactes sont imposés côté serveur. Le parcours et le lien de rétractation B2C ont été retirés. Le registre identifie SES et Postmark comme fournisseurs indirects de YoDevMail. `LEGAL_DOCUMENTS_APPROVED` et Checkout restent désactivés jusqu’aux validations juriste/comptable, à la confirmation de l’entité et au premier fan-out YoDevMail réel. |
| OAuth and secret envelopes | Integration pending | Provider-bound, expiring HMAC state protects Google Ads, Slack and Teams callbacks; Teams also uses PKCE. Versioned dual-read/single-write encryption and progressive rotation are implemented. A `kid` change creates one durable job per eligible workspace; Google/Teams refresh tokens, notification destinations, member emails, scheduled-report tokens and one-shot revelations are rewrapped with optimistic concurrency and a count-only audit. The imported staging envelope was rotated to a new `kid`, with dual-read removed after verification. Managed KMS custody and live Slack/Teams/Better Auth Google OAuth drills remain. |
| Google Ads v25 reads and MCC import | Integration pending | A historical real read-only v25 request succeeded from staging and returned three accessible customers with a Google request ID. The 2026-08-17 runtime read drill now fails safely before GAQL because the imported Google OAuth refresh token is revoked or expired. A fresh owner-authorized MCC connection, then Search/PMax/Shopping/conversion/read-error drills, are mandatory before any mutation rollout. |
| Safe approval pipeline | Implemented | State read/hash, safety rules, `validateOnly`, votes, atomic claim, drift, ambiguous reconciliation and audit exist. The complete persistence protocol is isolated behind a tested tenant service: source batching, vote/claim, drift, attempt numbering, strict `claimed → validated → submitted` compare-and-set, confirmed/ambiguous finalization, rejection and comments. A mutation is not sent if its durable execution record cannot advance, and ambiguous completion has a distinct audit event. Workspace approval policy is configurable within plan rules, and safety policies support deterministic campaign → client → workspace precedence with plan-gated scope. PostgreSQL 17 and staging Neon prove one winner under concurrent approval claims; controlled real Google mutation/drift/ambiguous-timeout evidence remains, with all writes disabled until then. |
| Google mutations | Implemented slice | Campaign status/budget, positive ad-group keywords, negative ad-group/campaign/account keywords, keyword/ad status, paused RSA draft, atomic Agency budget reallocation and heterogeneous 2–20 operation Agency batches exist. Account-wide negatives provision and attach a client-owned account list atomically when needed; manager-owned shared lists are rejected rather than mutated ambiguously. Generic batches accept reversible campaign status, campaign budget, keyword status and ad status changes only, force `partialFailure=false`, revalidate every source and reconcile the complete state. No automatic write or destructive delete exists. |
| Durable scheduler/jobs | Implemented | Leases, retries, dead-letter, manual retry and tenant isolation exist. Scheduler discovery is separated from provider-heavy job execution and serializes queries within its transaction. PostgreSQL 17 proves one worker lease/attempt under contention and concurrent schedulers create no duplicate work. Provider failure drills on staging remain. |
| Daily performance history | Implemented | Account and campaign daily series are collected separately from legacy rolling snapshots. Backfill quality must be checked after staging migration. |
| Alerts and notifications | Integration pending | Toutes les familles transactionnelles appellent désormais exclusivement `POST /v1/emails` de YoDevMail, une requête par destinataire, sans fallback direct. Un registre tenanté conserve clé métier, empreintes destinataire/contenu, UUID YoDevMail, tentatives, état et erreur ; timeout/2xx malformé restent ambigus et les 429/5xx retentables. Le webhook signé corrèle les événements hors ordre et donne priorité à la plainte/hard bounce. Le dépôt YoDevMail local accepte désormais `metadata.workspaceId` et le conserve comme tag interne; le schéma ciblé, ses tests et le typecheck passent. Chaque profil/catégorie FR/EN doit encore réussir une livraison réelle avant production. |
| Reports | Implemented slice | Public bearer links, 90-day expiry, OTP feedback, branding/host isolation and a shared HTML/PDF/CSV data model exist. Les rapports programmés et OTP utilisent le registre/idempotence YoDevMail. Branding logos are signature-checked PNG/JPEG/WebP files stored under a dedicated Vercel Blob prefix; arbitrary external URLs are never rendered. Validate Blob, YoDevMail and domain delivery on staging. |
| API v1 | Private beta only | Scopes, stable envelopes, key expiry, rate limits, IP pseudonymization, audit and cursor pagination exist. Elle a été retirée du pricing et des promesses commerciales. Même avec `PUBLIC_API_ENABLED=1`, seuls les workspaces internes ou explicitement listés dans `PRIVATE_API_WORKSPACE_IDS` peuvent créer/utiliser une clé. Authenticated HTTP staging E2E and rate-limit load evidence remain. API keys cannot approve or execute. |
| Export/deletion/retention | Integration pending | Private asynchronous ZIP export, 7-day artifacts, immediate tenant/API/reporting/Google revocation, J+30 purge and tombstones exist. La confirmation est strictement `SUPPRIMER` en FR et `DELETE` en EN. Une matrice de rétention unique alimente le job et la documentation : 90 jours pour livraisons YoDevMail et jobs terminés, conservation des pending/ambiguous/dead-letter et 24 mois pour métriques/snapshots/conversions/changements/approbations/audits, avec comptages et durée. Les migrations et protocoles de concurrence passent sur PostgreSQL jetable; les drills staging de restauration/tombstone, annulation contre purge et panne fournisseur restent obligatoires. |
| Sentry and structured evidence | Integration pending | Sentry server/edge/browser, release, source maps et redaction existent et les variables requises sont documentées. La console expose support, dead-letter, webhooks Stripe, réconciliations billing, livraisons YoDevMail et mutations ambiguës. Les runbooks couvrent désormais YoDevMail, scheduler/rétention, migration/rollback Vercel, remboursement et RGPD en plus des incidents existants. External dashboard/alert-provider validation, missed-cron monitoring, rehearsals and 30-day SLO evidence remain. |
| Custom domains | Integration pending | Agency DNS challenge, Vercel add/verify/config, HTTPS probe, host isolation, fallback and revoke exist. Host acceptance, generated links and scheduled emails re-check the current Agency/internal entitlement, so a downgrade immediately falls back to `ads.yodev.fr` even before provider cleanup. Validate against a staging Vercel project and certificate. |
| Automated verification | Partial | On 2026-08-18 the stabilization candidate passed the CI-equivalent `npm run check`: lint, types, zero-allowlist App Router data-boundary verification, transaction serialization, a 52-route production build, runtime audit and 767 tests across 116 files with 92.24% statements, 85.42% branches, 93.29% functions and 94.71% lines. Fifteen Python tests, Ruff and `pip-audit` are green. Six public E2E pass locally; the five authenticated scenarios deliberately skip without short-lived role states, while the release workflow now fails if any of the five states is absent. A disposable PostgreSQL 17 applies migrations through `0041` and proves 46-table RLS, four roles, 33 constraints, 15 invariants, concurrency and target load. Historical staging evidence still records a five-role page matrix 5/5, but the current staging deployment remains in maintenance and is not a current open-app E2E proof. Direct role action/API E2E, external provider/security drills and 30-day evidence remain launch gates. |

## Product Leadership (phases 8–12)

| Phase | Status | Implemented slice / remaining scope |
| --- | --- | --- |
| 8 — goals and pacing | Implemented | CPA/ROAS/volume/value targets, timezone-aware calendar pacing, forecast, Search budget/rank impression-share signals, minimum-volume recommendations and pacing/forecast vigies exist. Agency can convert a guarded 5% reallocation into an atomic two-budget approval batch; validate calculations with real staging accounts. |
| 9 — decline/tracking diagnostics | Implemented | v25 `change_event`, unified internal/Google timeline, 7/30/equivalent-weekday comparisons with coverage/confidence, conversion snapshots, offline-upload diagnostics and the Tag Assistant boundary exist. Causality is never asserted. Validate against real tracking histories. |
| 10 — optimization workflows | Implemented slice | Positive ad-group keywords and negative ad-group/campaign/account keywords, keyword/ad pause/reactivation, budget/status, paused RSA draft and atomic Agency budget reallocation use the generic approval pipeline. Account scope captures every observable campaign for the before/after measurement, is capped at 500 campaigns and rejects foreign or ambiguous shared lists. Structured impact previews expose no-op blockers, traffic interruption, shared-budget, campaign/account-wide negative-filter and paused-draft risks. Agency can combine 2–20 reversible heterogeneous proposals into one Google request with `partialFailure=false`; every source is re-read, drift-checked, safety-checked, validated, submitted once and reconciled atomically. Confirmed writes schedule a tenant-isolated 1–30 day before/after observation over real daily campaign data, with explicit incomplete-data outcomes. Controlled real-account evidence remains mandatory before rollout. |
| 11 — extended Google coverage | Implemented | Read-only v25 device, day/hour, geography, campaign/ad-group audiences, Auction Insights, Display/YouTube and PMax placements, asset groups, asset-level quantitative reporting, Shopping history and current product issue diagnostics exist with type-specific views. Creative trend compares two 15-day windows only above explicit volume thresholds and never asserts causality. Validate all GAQL contracts against a controlled real account before rollout. |
| 12 — professional agency workflow | Implemented slice | Branding with controlled logo upload, client feedback, custom domains, report templates and idempotent scheduled delivery exist. Report templates support optimistic-concurrency editing, immutable tenant-isolated versions, migration backfill, audit, export and safe deactivation while existing scheduled reports retain their snapshot. First-class tenant-isolated tasks cover manual work, alerts and approvals with roles, assignment, priorities, timezone-aware SLA/deadline, transitions, comments, audit and export. A Better Auth-backed member console applies plan quotas, four manageable roles, durable invitations, revocation/removal and owner transfer. Mentions and digests consentis utilisent désormais les clés métier et le registre YoDevMail. Richer team workload views remain an enhancement. |

## Phase 13 — public opening

Partial. A guided activation checklist now covers workspace → legal → Google → account → goal → vigie → report. Durable, tenant-isolated milestones record the first Google connection, account sync, analysis, monitor, report, legal acceptance and paid conversion, and are included in workspace exports. The public landing and organization onboarding are FR/EN. Durable FR/EN lifecycle emails cover welcome, trial days 7/12/14, payment success/failure and grace, refunds, cancellation, deletion and bilingual subprocessor notices through YoDevMail business keys. The operations console exposes dead-letter, failed Stripe webhooks, billing divergences, problematic YoDevMail deliveries and ambiguous mutations, with an audited manual Stripe reconciliation trigger. Keep invitation/public/Checkout/API flags disabled: authenticated provider E2E, external alert delivery, real provider drills and the 30-day beta exit evidence are not complete.

## Current deployment posture

Production must remain an internal Yodev deployment. The legacy US production database and production Vercel project have not been migrated or cut over.

The isolated staging foundation now exists and is operational:

- Vercel project `yodev-ads-staging`, current maintenance deployment `dpl_Gay5erHLEULCSfvxC2WEdz29aYCE`;
- Neon project `snowy-king-69942334`, PostgreSQL 17 in AWS Frankfurt, restricted app/system/purge/auth credentials and migrations through `0041`; rollback branch `backup-pre-0041-20260817` retained;
- two imported workspaces, with the real Yodev workspace explicitly provisioned as `internal` and the other kept `suspended`;
- historical Google Ads v25 read, unified Stripe sandbox catalogue and plan-change drill, database concurrency/load, restore drill, public E2E and the five-role page matrix completed; the current Google grant must be reconnected;
- all Google writes, Checkout, public beta, public API, notifications and custom domains remain fail-closed by environment switches.
- the stabilization candidate makes `/api/health` return `maintenance` or `degraded` with HTTP 503 when a promoted environment is in maintenance or its scheduler/retention workers are disabled; this behavior is not production evidence until the candidate is deployed;
- `.github/workflows/release-readiness.yml` is the manual promotion gate and requires complete provider configuration plus five ephemeral Playwright role states.

The remaining commercial blockers are not safely auto-certifiable from the repository:

1. provisionner les clés YoDevMail staging/production, approuver les profils transactionnels et réussir les recettes FR/EN, idempotence, timeout, bounce et plainte ;
2. reconnecter le MCC Google au staging, terminer les lectures réelles puis prouver les mutations contrôlées, la dérive et la réconciliation de timeout, workspace par workspace ;
3. fournir une session propriétaire staging (le drill Chrome du 2026-08-17 atteint bien la page de connexion mais aucun Google Sign-In n'y est configuré), puis terminer la matrice authentifiée directe Server Actions/API et les parcours FR/EN ;
4. activate and verify a French live Stripe account, prices, portal, refund flow and webhook, after professional tax/legal approval;
5. provision a Sentry staging/production project and prove redaction plus alert delivery;
6. reconnecter l'accès opérateur Neon à YoDevMail, sauvegarder et migrer sa base, puis provisionner sa clé et son webhook YoDevAds avec une identité AWS à privilèges minimaux ;
7. rehearse provider outage, incident and deletion-tombstone procedures, then collect the mandatory 30-day private-beta evidence.

Checkout and public opening must remain disabled until every item above has objective evidence.
