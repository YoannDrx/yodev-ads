# Ads by Yodev — implementation status

Last reviewed: 2026-08-13. This is the launch-readiness source of truth. A UI is not sufficient: “implemented” means the server contract, isolation and automated verification exist. External validation and rollout evidence are tracked separately.

Status meanings:

- **Implemented**: implemented in the repository and covered by automated checks.
- **Integration pending**: code is implemented; a real staging provider, migration, legal review, load test or operational drill is still mandatory.
- **Partial**: a useful slice exists, but the roadmap contract is incomplete.
- **Planned**: not implemented.

## Commercial Safe Launch (phases 0–7)

| Area | Repository status | Mandatory gate before commercial production |
| --- | --- | --- |
| Feature flags and kill switches | Implemented | Keep production flags off until each integration is validated. Global, per-workspace and independent status/budget/keyword/ad/batch mutation switches fail closed. |
| Dependencies, headers, CSP, audits | Implemented | Exercise CSP on every Better Auth/public route. CI now scans the full Git history with Gitleaks and publishes separate 30-day CycloneDX runtime SBOM artifacts for the web app and Python CLI. |
| Transactional Postgres and tenant repository | Implemented on staging | Interactive transactions and tenant/system roles exist. Runtime pools are cached per credential URL, bounded by `DATABASE_POOL_MAX`, and preserve typed Drizzle inference across Neon and the PostgreSQL integration adapter. A CI verifier forbids concurrent `Promise.all` work inside one transaction callback, preventing unsupported multi-query use of a single `pg` client. All 64 App Router source files — Server Actions, API v1, Google/Slack/Teams OAuth callbacks, export download, one-shot revelation, health and provider webhooks included — use repository/service methods and have no direct Drizzle/schema/transaction imports. The CI verifier runs with an empty allowlist. Member/invitation/ownership/task-preference, client-goal/workspace-settings/branding, complete MCC inventory, monitoring/alert workflow, tenant task workflow, billing checkout/audit, support/platform operations, Google approval/execution, API-key, notification-channel/OAuth, dead-letter retry, safety-policy, report-template, scheduled/public-report, OTP/feedback, subprocessor-change, custom-domain, export-request and deletion-lifecycle domains are behind tested services. Manual and post-billing MCC sync share one transactional implementation; sensitive resource mutations and their audits commit atomically. Invalid OTP attempts commit their counter before returning an error, verified-session lookup plus feedback persistence occur in one transaction, and a delayed Vercel verification cannot reactivate a concurrently revoked domain. A shared workspace-access advisory lock now serializes deletion with Google OAuth, API-key, report, connector and custom-domain creation; entitlements are re-read from the database inside that transaction. Purge and cancellation compete through atomic `purging`/`cancelling` claims, preventing Stripe reactivation from racing a destructive purge. Migrations through `0034` are applied to the isolated EU staging database; no production migration has been run by Codex. |
| RLS, DB roles, composite tenant constraints and immutable audit | Implemented | On 2026-08-13 a fresh PostgreSQL 17 and the EU Neon staging branch passed 46-table RLS/FORCE RLS checks, four no-`BYPASSRLS` roles including the isolated `yodev_auth` role, actor-isolated OAuth sessions, immutable-audit privileges, 33 composite constraints and 15 tenant/auth invariants. Migrations are current through `0034`. Better Auth obtains only a membership priority and member quota through two public-schema functions revoked from `PUBLIC`; both verify user membership, and the auth role still cannot query tenant workspace state directly. The restricted-role proof returns internal priority/limit for the member and neutral values for an outsider. Global subprocessor-change notices are denied to the application and purge roles and granted only to the system role. CI repeats the database protocols. Production rollback remains a cutover gate. |
| Better Auth identity and permission matrix | Integration pending | Clerk runtime code and dependencies are removed. Email/password with mandatory verification, magic link, Google sign-in, passkeys, password reset, sessions, organizations, five app roles, invitations, ownership transfer and session revocation are implemented. Invitation delivery is a durable, idempotent retrying job. The guarded one-time import was executed for both existing workspaces. On 2026-08-13 an ephemeral credential and a separate real Postmark magic link both proved staging sign-in, internal-workspace selection, dashboard, advertiser accounts and billing; all test credentials and sessions were deleted immediately afterward. Google sign-in callback registration and the complete five-role HTTP matrix remain before cutover. |
| Lifecycle, one-time trial and entitlements | Implemented | Page navigation, direct Server Actions, API keys and public reports fail closed by lifecycle state. Grace exposes stored views plus billing without Google calls; suspended/deletion states expose billing, export and deletion only. The one-time trial identity is migrated from the legacy Clerk identifier to the Better Auth user without making repeated trials possible. Test concurrent workspace creation and every state transition on authenticated staging. |
| Stripe lifecycle and tax fail-closed | Integration pending | Checkout reservation, idempotent/stale webhooks, grace/cancellation, fiscal guard, explicit checkout/invoice tax wording and successful partial/full refund handling exist. Checkout/legal evidence is committed atomically; customer attachment uses compare-and-set so a concurrent flow cannot replace an existing Stripe customer, and failed attempts release only their own reservation. Plan events reduce active Google accounts immediately and enqueue a durable full MCC refresh before any quota-based reactivation; Solo is normalized back to one approval. Out-of-order invoice events are checked against the authoritative subscription state; refunds are immutably audited, notify the billing contact and never suspend an otherwise active subscription. A real Stripe sandbox Test Clock covered activation, failed renewal, seven-day grace, recovery, scheduled cancellation, cancellation reversal and final cancellation with zero unprocessed webhooks. Live-account activation/KYC, refund evidence and professional tax review remain. |
| FR/EN and consent | Implemented slice | Public/legal content, landing, onboarding, Better Auth flows, metadata and the authenticated customer product are localized, including cockpit, analysis, generated findings, insights, history, approvals, alerts, tasks, reports, billing and settings. The locale chosen before signup is inherited atomically by the new workspace, can be changed by an administrator, and drives report OTP/lifecycle/task/support emails, alert emails, connector payloads and weekly digests. Legacy Server Action flash/error literals are translated centrally at render time; unknown provider diagnostics are preserved verbatim. The internal-only Yodev operations console remains intentionally French. Validate the complete English journey in authenticated staging E2E. |
| Commercial documents | Integration pending | CGV, privacy, cookies, subprocessors, DPA and withdrawal templates exist. An internal workflow enforces a full 15-day notice period for subprocessor changes, publishes the bilingual notice, fans out one durable/idempotent email job per commercial workspace (including suspended workspaces), records provider delivery evidence in each tenant audit and exposes failures through the dead-letter operations UI. `LEGAL_DOCUMENTS_APPROVED` and Checkout remain disabled. A consumer mediator, lawyer/accountant approval and the first real transactional-email fan-out remain mandatory. |
| OAuth and secret envelopes | Integration pending | Provider-bound, expiring HMAC state protects Google Ads, Slack and Teams callbacks; Teams also uses PKCE. Versioned dual-read/single-write encryption and progressive rotation are implemented. A `kid` change creates one durable job per eligible workspace; Google/Teams refresh tokens, notification destinations, member emails, scheduled-report tokens and one-shot revelations are rewrapped with optimistic concurrency and a count-only audit. The imported staging envelope was rotated to a new `kid`, with dual-read removed after verification. Managed KMS custody and live Slack/Teams/Better Auth Google OAuth drills remain. |
| Google Ads v25 reads and MCC import | Implemented | A real read-only v25 request succeeded from staging against the imported OAuth grant, returning three accessible customers and a Google request ID. Quota/error drills and production OAuth verification remain. |
| Safe approval pipeline | Implemented | State read/hash, safety rules, `validateOnly`, votes, atomic claim, drift, ambiguous reconciliation and audit exist. The complete persistence protocol is isolated behind a tested tenant service: source batching, vote/claim, drift, attempt numbering, strict `claimed → validated → submitted` compare-and-set, confirmed/ambiguous finalization, rejection and comments. A mutation is not sent if its durable execution record cannot advance, and ambiguous completion has a distinct audit event. Workspace approval policy is configurable within plan rules, and safety policies support deterministic campaign → client → workspace precedence with plan-gated scope. PostgreSQL 17 and staging Neon prove one winner under concurrent approval claims; controlled real Google mutation/drift/ambiguous-timeout evidence remains, with all writes disabled until then. |
| Google mutations | Implemented slice | Campaign status/budget, positive ad-group keywords, negative ad-group/campaign/account keywords, keyword/ad status, paused RSA draft, atomic Agency budget reallocation and heterogeneous 2–20 operation Agency batches exist. Account-wide negatives provision and attach a client-owned account list atomically when needed; manager-owned shared lists are rejected rather than mutated ambiguously. Generic batches accept reversible campaign status, campaign budget, keyword status and ad status changes only, force `partialFailure=false`, revalidate every source and reconcile the complete state. No automatic write or destructive delete exists. |
| Durable scheduler/jobs | Implemented | Leases, retries, dead-letter, manual retry and tenant isolation exist. Scheduler discovery is separated from provider-heavy job execution and serializes queries within its transaction. PostgreSQL 17 proves one worker lease/attempt under contention and concurrent schedulers create no duplicate work. Provider failure drills on staging remain. |
| Daily performance history | Implemented | Account and campaign daily series are collected separately from legacy rolling snapshots. Backfill quality must be checked after staging migration. |
| Alerts and notifications | Integration pending | Lifecycle, snooze, comments, assignment/deadline UI, durable delivery, FR/EN email/connector/digest rendering and per-vigie unresolved-incident reminders (4 h–30 days) exist. Every transactional email family now uses one Postmark-first/Resend-fallback transport with normalized recipients, provider evidence, deterministic business idempotency metadata, text alternatives and tracking disabled; a real Postmark magic link was sent and consumed successfully. Generic/legacy webhooks reject private/reserved IPv4/IPv6, pin delivery sockets to validated public DNS answers while preserving TLS SNI, never follow redirects and bound timeout/response size. Slack uses native `incoming-webhook` OAuth provisioning; Teams uses delegated Microsoft Graph OAuth, actor-bound temporary sessions, refresh-token rotation and native channel posting. Capabilities and channel quotas are re-evaluated at creation, dispatch and retry time. Validate a durable non-auth email and both connector providers with dedicated staging apps/tenants. |
| Reports | Implemented slice | Public bearer links, 90-day expiry, OTP feedback, branding/host isolation and a shared HTML/PDF/CSV data model exist. Reusable, editable and immutably versioned FR/EN templates plus durable weekly/monthly delivery use timezone-aware scheduling, encrypted bearer tokens, Resend idempotency, delivery leases, custom-domain URLs, immediate revoke and token rotation. Branding logos are signature-checked PNG/JPEG/WebP files stored under a dedicated Vercel Blob prefix; arbitrary external URLs are never rendered. Validate Blob, Resend and domain delivery on staging. |
| API v1 | Implemented slice | Scopes, stable envelopes, key expiry, rate limits, IP pseudonymization, audit and cursor pagination exist. Real PostgreSQL load verification proves four stable 25-row pages across 100 approvals without skip/duplication. Authenticated HTTP staging E2E and rate-limit load evidence remain. API keys cannot approve or execute. |
| Export/deletion/retention | Implemented | Private asynchronous ZIP export, 7-day artifacts, immediate tenant/API/reporting/Google revocation, J+30 purge and tombstones exist. The purge removes controlled logos, Vercel domains and the Better Auth organization before SQL deletion while preserving the one-time trial record. Run restore/tombstone and provider-backup drills. |
| Sentry and structured evidence | Integration pending | Sentry redaction and request/job/provider identifiers exist in core paths. Internal dashboards expose exact support/dead-letter/Stripe/mutation counts. Any tenant or system job reaching dead-letter and every failed Stripe webhook enqueue a redacted, deduplicated operations email; the alert job cannot recursively alert on itself and obeys the notification kill switch. Runbooks now cover OAuth/developer-token leaks, Google/Stripe outages, bad mutations, DB restore/tombstones, envelope-key rotation, notification incidents and multi-tenant incidents with RPO 15 min/RTO 4 h evidence. External dashboard/alert-provider validation, rehearsals and 30-day SLO evidence remain. |
| Custom domains | Integration pending | Agency DNS challenge, Vercel add/verify/config, HTTPS probe, host isolation, fallback and revoke exist. Host acceptance, generated links and scheduled emails re-check the current Agency/internal entitlement, so a downgrade immediately falls back to `ads.yodev.fr` even before provider cleanup. Validate against a staging Vercel project and certificate. |
| Automated verification | Partial | Lint, types, zero-allowlist App Router data-boundary verification, transaction-serialization verification, a 52-route production build, 636 unit tests across 109 files, 15 Python tests, zero known Python vulnerabilities, zero high runtime npm vulnerabilities and 6 public E2E are green. A disposable PostgreSQL 17 and EU Neon staging apply all migrations and prove RLS/roles/constraints/invariants plus concurrency/lifecycle protocols. The load suite provisions 100 workspaces, 149 advertiser accounts including one 50-account Agency, 200 monitors, 10,000 notification deliveries and 100 approvals; stable API pagination and zero duplication/leak were observed, with a report-burst p95 of 8.648 s still worth optimizing. A Neon restore branch recovered 2 workspaces, 1 auth user and all then-current migrations and was destroyed after the drill. Basic authenticated staging E2E and a consumed real magic link are green; the full five-role matrix, external security load, provider failure drills and 30-day evidence remain launch gates. |

## Product Leadership (phases 8–12)

| Phase | Status | Implemented slice / remaining scope |
| --- | --- | --- |
| 8 — goals and pacing | Implemented | CPA/ROAS/volume/value targets, timezone-aware calendar pacing, forecast, Search budget/rank impression-share signals, minimum-volume recommendations and pacing/forecast vigies exist. Agency can convert a guarded 5% reallocation into an atomic two-budget approval batch; validate calculations with real staging accounts. |
| 9 — decline/tracking diagnostics | Implemented | v25 `change_event`, unified internal/Google timeline, 7/30/equivalent-weekday comparisons with coverage/confidence, conversion snapshots, offline-upload diagnostics and the Tag Assistant boundary exist. Causality is never asserted. Validate against real tracking histories. |
| 10 — optimization workflows | Implemented slice | Positive ad-group keywords and negative ad-group/campaign/account keywords, keyword/ad pause/reactivation, budget/status, paused RSA draft and atomic Agency budget reallocation use the generic approval pipeline. Account scope captures every observable campaign for the before/after measurement, is capped at 500 campaigns and rejects foreign or ambiguous shared lists. Structured impact previews expose no-op blockers, traffic interruption, shared-budget, campaign/account-wide negative-filter and paused-draft risks. Agency can combine 2–20 reversible heterogeneous proposals into one Google request with `partialFailure=false`; every source is re-read, drift-checked, safety-checked, validated, submitted once and reconciled atomically. Confirmed writes schedule a tenant-isolated 1–30 day before/after observation over real daily campaign data, with explicit incomplete-data outcomes. Controlled real-account evidence remains mandatory before rollout. |
| 11 — extended Google coverage | Implemented | Read-only v25 device, day/hour, geography, campaign/ad-group audiences, Auction Insights, Display/YouTube and PMax placements, asset groups, asset-level quantitative reporting, Shopping history and current product issue diagnostics exist with type-specific views. Creative trend compares two 15-day windows only above explicit volume thresholds and never asserts causality. Validate all GAQL contracts against a controlled real account before rollout. |
| 12 — professional agency workflow | Implemented slice | Branding with controlled logo upload, client feedback, custom domains, report templates and idempotent scheduled delivery exist. Report templates support optimistic-concurrency editing, immutable tenant-isolated versions, migration backfill, audit, export and safe deactivation while existing scheduled reports retain their snapshot. First-class tenant-isolated tasks cover manual work, alerts and approvals with roles, assignment, priorities, timezone-aware SLA/deadline, transitions, comments, audit and export. A Better Auth-backed member console applies plan quotas, four manageable roles, durable invitations, revocation/removal and owner transfer. Members register a unique mention handle backed by their verified email; consented mention notifications and timezone-aware daily/weekly personal digests run through the durable queue with Resend idempotency. Richer team workload views remain an enhancement. |

## Phase 13 — public opening

Partial. A guided activation checklist now covers workspace → legal → Google → account → goal → vigie → report. Durable, tenant-isolated milestones record the first Google connection, account sync, analysis, monitor, report, legal acceptance and paid conversion, and are included in workspace exports. The public landing and organization onboarding are FR/EN; onboarding persists the chosen locale into the workspace instead of silently falling back to French. Durable FR/EN lifecycle emails cover welcome, trial days 7/12/14, payment success/failure and grace, refunds, cancellation, deletion and bilingual subprocessor-change notices; they use verified owner/billing recipients, Resend idempotency and the notification kill switch without consuming queued work. The public subprocessor register displays announced changes and their effective dates. A public FR/EN status page now fails visibly when status storage is unavailable; bilingual platform incidents and 15-day subprocessor notices are controlled from an internal-only operations console. Tenant support tickets include audited customer/support conversations, internal notes hidden by RLS and exports, rate limits, durable notifications and a cross-tenant Yodev queue. The internal console exposes exact activation-funnel counts, 12-week signup cohorts, median time-to-report/time-to-paid and critical dead-letter, Stripe and mutation signals. Keep invitation/allowlist and public flags disabled: authenticated provider E2E, external alert-delivery validation, longer-term retention analytics and the 30-day beta exit evidence are not complete.

## Current deployment posture

Production must remain an internal Yodev deployment. The legacy US production database and production Vercel project have not been migrated or cut over.

The isolated staging foundation now exists and is operational:

- Vercel project `yodev-ads-staging`, current verified deployment `dpl_Fm6VoKPCvBcU8J36JM5oGvK7rXgN`;
- Neon project `snowy-king-69942334`, PostgreSQL 17 in AWS Frankfurt, restricted app/system/purge/auth credentials and migrations through `0034`;
- two imported workspaces, with the real Yodev workspace explicitly provisioned as `internal` and the other kept `suspended`;
- real Google Ads v25 read, Stripe sandbox lifecycle, database concurrency/load, restore drill, public E2E and basic authenticated E2E completed;
- all Google writes, Checkout, public beta, public API, notifications and custom domains remain fail-closed by environment switches.

The remaining commercial blockers are not safely auto-certifiable from the repository:

1. configure the Better Auth Google callback if Google sign-in is retained, then run the complete five-role authenticated HTTP matrix;
2. prove controlled Google mutations, drift and timeout reconciliation tenant by tenant;
3. activate and verify the live Stripe account, prices, portal, refund flow and webhook, after professional tax/legal approval;
4. finish the Sentry Marketplace installation after the account owner accepts its terms, then prove redaction plus alert delivery;
5. rehearse provider outage, incident and deletion-tombstone procedures, then collect the mandatory 30-day private-beta evidence.

Checkout and public opening must remain disabled until every item above has objective evidence.
